//The unshift() method adds an element at the beginning of the array. For example,

let student = ['class:b.tech' , 'age:21']

//add element at the begin

student.unshift('name:virendra' , 'branch:computer science');

console.log(student);